import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { BitbucketStateInterface } from './state';

const getters: GetterTree<BitbucketStateInterface, StateInterface> = {
  // someAction (/* context */) {
  //   // your code
  // }
};

export default getters;
